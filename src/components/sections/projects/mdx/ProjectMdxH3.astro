---
interface Props {
	id?: string;
	class?: string;
}

const { id, class: className } = Astro.props;
---

<h3
	id={id}
	class:list={[
		'mt-12 scroll-mt-28 text-title-h4 font-semibold leading-tight text-text-900 dark:text-text-50',
		className
	]}
>
	<slot />
</h3>
