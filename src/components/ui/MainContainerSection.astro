---
interface Props {
	id?: string;
	class?: string;
}

const { id, class: className } = Astro.props;
---

<section id={id} class:list={['mx-auto max-w-6xl px-4 py-16 sm:px-6 sm:py-24 lg:px-8 lg:py-12', className]}>
	<slot />
</section>
